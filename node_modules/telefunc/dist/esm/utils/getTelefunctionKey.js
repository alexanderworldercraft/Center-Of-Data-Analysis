import { assert } from './assert';
export function getTelefunctionKey(filePath, exportName) {
    assert(!exportName.includes(':'));
    const telefunctionKey = filePath + ':' + exportName;
    return telefunctionKey;
}
