export { assertManifest };
export type { Manifest };
type Manifest = {
    version: string;
    config: Record<string, unknown>;
};
declare function assertManifest(manifest: Record<string, unknown>): asserts manifest is Manifest;
