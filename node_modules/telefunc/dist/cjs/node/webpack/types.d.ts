export type { Loader };
export type { Compiler };
type Loader = {
    _compiler: Compiler;
    resource: string;
    mode: 'production' | 'development';
};
type Compiler = {
    name: string;
    context: string;
    hooks: any;
};
